{
  "builds": [
    {
      "src": "mutts/wsgi.py",
      "use": "@vercel/python",
      "config": { 
        "maxLambdaSize": "50mb",
        "maxDuration": 60
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "mutts/wsgi.py" }
  ]
}